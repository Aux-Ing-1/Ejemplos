$(document).ready(function() {
    $('#table').DataTable( {
        /* Some configurations */
        paging: true,
        scrollY: 400,
        // scrollX: 400,
        searching: true,
        ordering: true,
        select: true
} );
} );